$(function(){
    /* Do some javascripts on page load! */
    if (typeof console !== 'undefined' && typeof console.log === 'function') {
        console.log('Yay! my javascript file is loaded!');
    }
});
